---
sidebar_position: 6
---

# Import Tasks editor

The **Import Tasks** Webview editor is the detailed table surface for ingestion and import operations. Open it from the Import Tasks sidebar section, the [EE Import panel](../panels/ee-import), or the Command Palette.

It streams pages as they load, supports sortable columns and pagination, then silently refreshes non-terminal operations every 15 seconds. Rows include cancellation, destination information where available, and preview actions for resulting assets.

<div className="screenshot-placeholder" role="note">
  <strong>Import Tasks editor screenshot</strong>
  <span>Show filter controls, task table, progress, and pagination.</span>
</div>